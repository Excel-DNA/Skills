# Minimal Excel-DNA C# Add-in — Modern .NET

Use this when the target machines can have the required .NET Desktop Runtime installed and modern .NET features matter.

Build the project, load the generated `.xll`, and test:

```excel
=MY.ADD(1,2)
```
