# Minimal Excel-DNA C# Add-in — .NET Framework

Use this when broad Windows/Excel distribution stability matters more than newest .NET features.

Build the project, load the generated `.xll`, and test:

```excel
=MY.ADD(1,2)
```
